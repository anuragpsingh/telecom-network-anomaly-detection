# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
.eggs/
dist/
build/
.env
.venv/
venv/

# Data (large generated files — run main.py to regenerate)
data/telemetry.csv
data/anomaly_labels.csv

# Checkpoints are committed so users can run inference without retraining.
# Only exclude files that are not part of the pre-trained release.
checkpoints/transformer_model.pt

# Results (plots & CSVs — regenerated by main.py)
results/

# macOS
.DS_Store

# Jupyter
.ipynb_checkpoints/

# IDEs
.vscode/
.idea/
